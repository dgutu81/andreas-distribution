import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {supabase} from '../lib/supabaseClient'
export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[err,setErr]=useState('');const nav=useNavigate();async function login(e){e.preventDefault();const{error}=await supabase.auth.signInWithPassword({email,password});if(error)setErr(error.message);else nav('/admin')}return <section className="min-h-screen grid place-items-center bg-cream px-4"><form onSubmit={login} className="card p-8 w-full max-w-md"><h1 className="font-serif text-4xl mb-6">Admin login</h1>{err&&<p className="bg-red-50 text-red-700 p-3 rounded-xl mb-4">{err}</p>}<label className="label">Email</label><input className="field mb-4" value={email} onChange={e=>setEmail(e.target.value)}/><label className="label">Parolă</label><input type="password" className="field mb-6" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn btn-dark w-full">Intră</button></form></section>}
